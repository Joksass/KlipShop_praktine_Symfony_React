{% extends 'base.html.twig' %}

{% block title %}Hello LuckyController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <form action="{{ path('app_logout') }}" method="post">
            <button type="submit">Logout</button>
        </form>
    {% else %}
        <a href="{{ path('app_login') }}">Login</a>
        <a href="{{ path('app_register') }}">Register</a>
    {% endif %}

    <h1>Hello {{ controller_name }}! ✅</h1>
    <h2>Your lucky number is: {{ lucky }}!</h2>
    
    {# CONTROLLERI PASIRINKTAS VIENAS DUOMO IS DB IR ISVESTAS TWIGE #}
    <h3> {{ myname }} </h3>

    {# IS DB DUOMENU ISVEDAMAS VIENAS BE FORO #}
    {% set pirmasDuomo = test[0] %}
    <h3> {{ pirmasDuomo.surname }} </h3>

    {# VISI DUOMENIS SU FORU #}
    {% for testas in test %}
        <h4>{{ testas.id }} {{ testas.name }} {{ testas.surname }} </h4>
    {% endfor %}



    

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/xampp/htdocs/KlipShop_praktine_Symfony_React/src/Controller/LuckyController.php'|file_link(0) }}">src/Controller/LuckyController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/xampp/htdocs/KlipShop_praktine_Symfony_React/templates/lucky/index.html.twig'|file_link(0) }}">templates/lucky/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
